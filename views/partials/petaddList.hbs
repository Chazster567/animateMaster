{{!-- Pet List --}}
{{#if petsExist}}
{{#each pets}}
<div class="col-lg-4 col-xs-12 carded-inner">
    <h2>{{this.name}}</h2>
    <p>{{this.age}}</p>
    <p>{{this.breed}}</p>
</div>
{{/each}}
{{else}}
<div class="col-xs-12 text-center">
    <h1>No Pets</h1>
</div>
{{/if}}